'use strict';

/**
 * remote-support controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::remote-support.remote-support');
